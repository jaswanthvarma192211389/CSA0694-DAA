#include<stdio.h>
#include<conio.h>

int fibonacci(int n){
	if(n<=1)
	  return n;
    else
      return (fibonacci(n-1)+fibonacci(n-2));
      
}
int main(){
	int n1=0,n2=1,n3,num;
	printf("enter no of terms: ");
	scanf("%d",&num);
	for(int i=0;i<num;i++){
		printf("%d ",fibonacci(i));
	}
}
